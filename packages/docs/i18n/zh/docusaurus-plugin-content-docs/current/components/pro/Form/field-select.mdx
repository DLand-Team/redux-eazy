---
sidebar_position: 1
---

# Field Select



import {PropsTable} from '@site/src/components/PropsTable';
import BrowserWindow from '@site/src/components/BrowserWindow';
import CodeBlock from '@theme/CodeBlock';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import {FieldSelectDemo,FieldTextProps,FormProvider,FieldSelect,NotifyProvider} from '@site/src/theme/ReactLiveScope/components'

<NotifyProvider>
 <Tabs
  defaultValue="Demo"
  values={[
    {label: 'Demo', value: 'Demo'},
    {label: 'PlayGround', value: 'PlayGround'},
  ]}>
  <TabItem value="PlayGround">

```jsx live noInline
const Demo = () => {
  const formRef = useFormRef({});
  const { handleSubmit } = formRef;
  const onSubmit = handleSubmit(() => {
    notify.success(JSON.stringify(formRef.getValues()));
  });
  const options = ["1", "2"];
  return (
    <FormProvider formRef={formRef} onSubmit={onSubmit}>
      <FieldSelect label="Field" name="Field">
        {options?.map((item: any) => {
          return (
            <MenuItem
              key={typeof item == "string" ? item : item.key}
              value={typeof item == "string" ? item : item.value}>
              {typeof item == "string" ? item : item.label}
            </MenuItem>
          );
        })}
      </FieldSelect>
      <Stack
        direction="row"
        sx={{
          justifyContent: "end",
        }}>
        <Button
          sx={{
            marginTop: "10px",
          }}
          variant="contained"
          type="submit">
          Submit
        </Button>
      </Stack>
    </FormProvider>
  );
};

render(<Demo />);
```

</TabItem>
<TabItem value="Demo">

<BrowserWindow>
  <FieldSelectDemo />
</BrowserWindow>

</TabItem>
</Tabs>
</NotifyProvider>

<details>
  <summary>show code</summary>
<Tabs>
<TabItem value="componennt">

```tsx {5,8,12,41-61} showLineNumbers
import { Button, Stack } from "@mui/material";
import PropsTable from "@site/src/components/PropsTable";
import { NotifyProvider } from "mui-eazy";
import {
  useFormRef,
  FieldDatePicker,
  FormProvider,
  notify,
  LocalizationProvider,
} from "mui-eazy";

export const FieldSelectDemo = () => {
  const formRef = useFormRef({});
  const { handleSubmit } = formRef;
  const onSubmit = handleSubmit(() => {
    notify.success(JSON.stringify(formRef.getValues()));
  });
  const options = ["1", "2"];
  return (
    <FormProvider formRef={formRef} onSubmit={onSubmit}>
      <FieldSelect name="FieldMul">
        {options?.map((item: any) => {
          return (
            <MenuItem
              key={typeof item == "string" ? item : item.key}
              value={typeof item == "string" ? item : item.value}>
              {typeof item == "string" ? item : item.label}
            </MenuItem>
          );
        })}
      </FieldSelect>
      <Stack
        direction="row"
        sx={{
          justifyContent: "end",
        }}>
        <Button
          sx={{
            marginTop: "10px",
          }}
          variant="contained"
          type="submit">
          Submit
        </Button>
      </Stack>
    </FormProvider>
  );
};
```

</TabItem>
</Tabs>

</details>

## Props {#npm2yarn-remark-plugin-configuration}

:::tip tip

- Type: any, can be customized as needed.
- Default value:
  - Not specified: optional.
  - Specified as "require": mandatory.
- Type link: Click to navigate to the details below.

:::

## Type

<FieldTextProps></FieldTextProps>
